/* AUTOMATICALLY GENERATED by qapi-gen.py DO NOT MODIFY */

/*
 * Schema-defined QAPI/QMP events
 *
 * Copyright (c) 2014 Wenchao Xia
 * Copyright (c) 2015-2018 Red Hat Inc.
 *
 * This work is licensed under the terms of the GNU LGPL, version 2.1 or later.
 * See the COPYING.LIB file in the top-level directory.
 */

#include "qemu/osdep.h"
#include "qapi-emit-events.h"
#include "qapi-events-vfio.h"
#include "qapi-visit-vfio.h"
#include "qapi/compat-policy.h"
#include "qapi/error.h"
#include "qapi/qmp/qdict.h"
#include "qapi/qmp-event.h"

void qapi_event_send_vfio_migration(const char *device_id, const char *qom_path, VfioMigrationState device_state)
{
    QDict *qmp;
    QObject *obj;
    Visitor *v;
    q_obj_VFIO_MIGRATION_arg param = {
        (char *)device_id, (char *)qom_path, device_state
    };

    qmp = qmp_event_build_dict("VFIO_MIGRATION");

    v = qobject_output_visitor_new_qmp(&obj);

    visit_start_struct(v, "VFIO_MIGRATION", NULL, 0, &error_abort);
    visit_type_q_obj_VFIO_MIGRATION_arg_members(v, &param, &error_abort);
    visit_check_struct(v, &error_abort);
    visit_end_struct(v, NULL);

    visit_complete(v, &obj);
    if (qdict_size(qobject_to(QDict, obj))) {
        qdict_put_obj(qmp, "data", obj);
    } else {
        qobject_unref(obj);
    }
    qapi_event_emit(QAPI_EVENT_VFIO_MIGRATION, qmp);

    visit_free(v);
    qobject_unref(qmp);
}

/* Dummy declaration to prevent empty .o file */
char qapi_dummy_qapi_events_vfio_c;
